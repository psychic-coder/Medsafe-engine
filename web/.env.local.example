# Copy to .env.local. Base URL of the medsafe-engine FastAPI service.
# Must be reachable from the *browser*, and must appear in the API's CORS_ALLOW_ORIGINS.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
