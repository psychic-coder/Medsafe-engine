"""API route modules.

One module per endpoint group: ``resolve`` (drug string to molecule and substitutes), ``check``
(interaction checking across a prescription), and ``health`` (liveness and graph readiness).
Routers are registered by ``medsafe.api.main``.

# TODO: implement in Phase 5
"""
