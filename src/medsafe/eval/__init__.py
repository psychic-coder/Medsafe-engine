"""Evaluation layer.

Built alongside each phase, not bolted on at the end. ``golden_set`` holds the labelled fixtures,
``metrics`` defines what is measured, and ``harness`` runs the engine against the set and reports.

# TODO: implement alongside Phases 2, 4, 5
"""
